# Q-learning-Maze-Solver
A Python/Pygame Q-learning demo that generates perfect mazes of any size, trains three agents to find optimal paths, and visualizes learning with a dynamic heatmap, adjustable speed controls, and side-panel stats. Maze dimensions auto-scale without breaking the UI.
